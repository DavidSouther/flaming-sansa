svg(
    id="graphingTooltip"
    width="100%"
    height="100%"
)
    g(
        ng-attr-visibility="{{ tooltipData.show ? 'visible' : 'hidden' }}"
        style="background: peach;"
    )
        text(
            at="tooltipData.position"
        ) {{ tooltipData.text }}
